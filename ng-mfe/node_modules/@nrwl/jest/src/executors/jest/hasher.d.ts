import { NxJsonConfiguration, ProjectGraph, Task, TaskGraph, WorkspaceJsonConfiguration } from '@nrwl/devkit';
import { Hash, Hasher } from '@nrwl/workspace/src/core/hasher/hasher';
export default function run(task: Task, context: {
    hasher: Hasher;
    projectGraph: ProjectGraph;
    taskGraph: TaskGraph;
    workspaceConfig: WorkspaceJsonConfiguration & NxJsonConfiguration;
}): Promise<Hash>;
